<Window x:Class="EffectApk.UI.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EffectAPK — первичная настройка"
        Width="620" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock TextWrapping="Wrap">
            EffectAPK скачает и настроит локальный Android-рантайм: Android SDK
            (platform-tools, headless-эмулятор, AOSP-образ без Google Play) и scrcpy.
            Это делается один раз и требует ~3–4 ГБ на диске и Java 17+.
            Сетевые загрузки: только dl.google.com и github.com.
        </TextBlock>

        <TextBlock x:Name="StatusText" Grid.Row="1" Margin="0,12,0,4"
                   FontWeight="SemiBold" Text="Готов к установке." />

        <ProgressBar x:Name="Progress" Grid.Row="2" Height="8" Minimum="0" Maximum="1" />

        <TextBox x:Name="LogBox" Grid.Row="3" Margin="0,12,0,12"
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11" TextWrapping="NoWrap" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="StartButton" Content="Начать установку" Padding="16,6" Click="StartButton_Click" />
            <Button x:Name="CancelButton" Content="Отмена" Padding="16,6" Margin="8,0,0,0" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
